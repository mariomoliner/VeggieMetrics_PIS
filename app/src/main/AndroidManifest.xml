<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.mmolinca20alumnes.veggiemetrics">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".profileFragment" />
        <activity android:name=".testSetmanal"
            android:parentActivityName=".profileFragment">
        </activity>
        <activity android:name=".NewRecipe" />
        <activity android:name=".recipe" />
        <activity android:name=".Register" />
        <activity android:name=".Rec_password_activity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".perfil_activity" />
    </application>

</manifest>